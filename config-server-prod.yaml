# =========================
# Config Server – PROD
# =========================

spring:
  rabbitmq:
    host: rabbitmq-prod
    port: 5672
    username: guest
    password: guest

logging:
  level:
    root: WARN
    org.springframework.cloud.config: INFO
    org.springframework.web: WARN

management:
  endpoint:
    health:
      show-details: never
